node_modules/
dist/
.env
*.log
.venv/
.claude/hooks/.state/

# Confidential — contains PAN, DOB, home address, and is confidential per its own Clause 12.5.
# Keep this out of the (public) repo.
Integration Agreement*.pdf
